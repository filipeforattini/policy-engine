{
  "name": "policy-engine",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "examples": "ts-node src/examples/run.ts",
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node-dev --respawn --transpile-only src/examples/run.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
